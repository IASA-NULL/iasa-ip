# .travis.yml
language: node_js

node_js:
  - "lts/*"

os: windows

branches:
  only:
    - gh-pages
    - /.*/

install:
  - npm install

cache:
  directories:
    - node_modules
    - $HOME/.cache/electron
    - $HOME/.cache/electron-builder

script:
  - npm run-script dist

deploy:
  local-dir: C:\Users\travis\build\IASA-Null\iasa-ip\build\IP.exe
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master